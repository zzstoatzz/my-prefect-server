apiVersion: apps/v1
kind: Deployment
metadata:
  name: hub
  namespace: prefect
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hub
  template:
    metadata:
      labels:
        app: hub
    spec:
      imagePullSecrets:
        - name: atcr-creds
      containers:
        - name: hub
          image: atcr.io/zzstoatzz.io/hub:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          env:
            - name: DUCKDB_PATH
              value: /analytics/analytics.duckdb
          volumeMounts:
            - name: analytics
              mountPath: /analytics
              readOnly: true
          resources:
            requests:
              # raised from 64Mi: steady-state sits ~190Mi after duckdb mmaps the
              # analytics db, so 64Mi was a misleading scheduler reservation.
              memory: 192Mi
              cpu: 50m
            limits:
              # 256Mi → 768Mi cut OOM rate from 96/16h to 3/41h, but query
              # plans on the analytics db still spike past 768Mi every ~14h.
              # node has 8GB and sits at 44%, so 1.5Gi is well within budget.
              memory: 1536Mi
              cpu: 500m
      volumes:
        - name: analytics
          hostPath:
            path: /var/lib/prefect-analytics
            type: Directory
---
apiVersion: v1
kind: Service
metadata:
  name: hub
  namespace: prefect
spec:
  selector:
    app: hub
  ports:
    - port: 3000
      targetPort: 3000
